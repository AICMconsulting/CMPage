export type { EmailData, ValidationResult, EmailResult } from './types';
export { emailConfig } from './config';
export { validateEmailData } from './validation';
export { initEmailService, sendContactForm } from './service';